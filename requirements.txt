# micrograd runtime dependencies
# Install inside the pinned Docker image: dolfinx/dolfinx:v0.7.3
# All FEniCSx dependencies (dolfinx, ufl, basix, ffcx, petsc4py, mpi4py)
# are pre-installed in the Docker image.
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
nlopt>=2.7
