# brinkgrad runtime dependencies
# Install inside the pinned Docker image: dolfinx/dolfinx:v0.7.3
# FEniCSx dependencies (dolfinx, ufl, basix, petsc4py, mpi4py)
# are pre-installed in the Docker image.
numpy>=1.23.2
scipy>=1.11.4
matplotlib>=3.8.3
nlopt>=2.7.1
