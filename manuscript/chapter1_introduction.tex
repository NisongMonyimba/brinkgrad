\section{Introduction}
\label{sec:introduction}

Density-based topology optimisation for Stokes flow, introduced by
Borrvall and Petersson~\cite{borrvall2003}, has been extended to
conjugate heat transfer~\cite{alexandersen2014}, scalar transport
problems~\cite{makhija2015}, and unsteady flows~\cite{kreissl2011}.
The Brinkman--SIMP approach replaces solid regions with a high-permeability
porous medium, enabling gradient-based optimisation of fluid topology
without remeshing.
Microfluidic systems provide a natural benchmark application because the
small length scales of soft-lithography devices make manual re-design
expensive and simulation-driven optimisation increasingly accessible~\cite{whitesides2006}.

A reusable framework for coupled flow-transport topology optimisation
in FEniCSx must satisfy five requirements:

\subsection*{Software requirements}

\textbf{R1.~Coupled physics.}
Solve Brinkman flow and convection-diffusion transport simultaneously
with a concentration-mismatch objective.
Existing FEniCSx codes target pure flow~\cite{borrvall2003}
or structural problems~\cite{dokken2023};
coupled flow-transport requires additional implementation effort.

\textbf{R2.~High-P\'{e}clet stability.}
Stabilise both forward and adjoint transport at
$\Pe\sim10^2$--$10^5$ (microfluidic operating regime).
Existing FEniCSx topology optimisation~\cite{dokken2023}
operates at $\Pe\ll1$ and does not require stabilisation.

\textbf{R3.~Adjoint sensitivity.}
Compute $\mathrm{d}J/\mathrm{d}\rho$ in two PDE solves
per iteration, independently of the number of design variables.

\textbf{R4.~End-to-end reproducibility.}
Provide Docker, CI, and versioned archival so that every result
(Section~\ref{sec:reproducibility}).
Published microfluidic topology optimisation results~\cite{na2022,feppon2024}
are not accompanied by open-source code.

\textbf{R5.~Extensibility.}
Allow users to change objectives, targets, and constraints
with minimal code changes.

Table~\ref{tab:comparison} compares existing frameworks against these requirements;
\texttt{brinkgrad} is designed to meet all five out-of-the-box.
No openly documented FEniCSx implementation combining all five
features has been reported, to the authors' knowledge.


\begin{table}[h]
\centering\scriptsize
\caption{Features included in the distributed repositories
         at the time of writing.
         $\checkmark$~=~included in distributed package;
         custom~=~user implementation required;
         partial~=~partially available.
         All frameworks are extensible and can support additional
         capabilities through user implementation.}
\label{tab:comparison}
\begin{tabular}{@{}p{3.8cm}p{2.1cm}p{1.9cm}p{1.9cm}@{}}
\toprule
\textbf{Workflow capability} &
\textbf{dolfin-adj.}~\cite{farrell2013} &
\textbf{cashocs}~\cite{blauth2023} &
\textbf{brinkgrad} \\
\midrule
\multicolumn{4}{@{}l}{\textit{Framework type}} \\
Adjoint/optimisation framework & \checkmark & \checkmark & \checkmark \\
Built-in topology optimisation & custom & custom & \checkmark \\
\midrule
\multicolumn{4}{@{}l}{\textit{Physics implementation}} \\
Generic adjoint framework      & \checkmark & \checkmark & \checkmark \\
Coupled Brinkman--CD solver    & custom      & custom      & \checkmark \\
SUPG-stabilised adjoint        & custom      & custom      & \checkmark \\
Concentration-mismatch obj.    & custom      & custom      & \checkmark \\
\midrule
\multicolumn{4}{@{}l}{\textit{Ready-to-run resources}} \\
Pre-configured benchmark       & partial     & partial     & \checkmark \\
Example scripts (7+)           & partial     & \checkmark & \checkmark \\
Jupyter tutorial notebook      & partial     & partial     & \checkmark \\
\midrule
\multicolumn{4}{@{}l}{\textit{Reproducibility infrastructure}} \\
Pinned Docker image            & custom      & custom      & \checkmark \\
CI regression tests            & custom      & custom      & \checkmark \\
Zenodo DOI archive             & custom      & custom      & \checkmark \\
Single-command reproduction    & custom      & custom      & \checkmark \\
\bottomrule
\end{tabular}
\end{table}

\subsection*{Intended use}

\texttt{brinkgrad} is intended as a reproducible reference implementation
and educational framework for coupled flow--transport topology optimisation
in FEniCSx.
The package prioritises transparency, validation, and reproducibility
over algorithmic novelty.

\subsection*{Intended contribution}

\texttt{brinkgrad} is an open-source FEniCSx framework
for topology optimisation of coupled Brinkman--convection-diffusion
systems targeting concentration-gradient generation
in microfluidic channels.
The framework contributes SUPG-stabilised adjoint sensitivity
for coupled flow-transport at high P\'{e}clet numbers,
integrated CI-based verification, and single-command Docker
reproducibility --- capabilities not collectively provided
in any documented open-source FEniCSx topology optimisation package
known to the authors (Table~\ref{tab:comparison}).

\subsection*{Contributions}

\texttt{brinkgrad} provides the first openly documented,
CI-verified, and Docker-reproducible FEniCSx implementation
of coupled Brinkman--convection-diffusion topology optimisation
with SUPG-stabilised adjoint transport.
The package is suitable for research, education, and
as a reusable baseline for related PDE-constrained
optimisation workflows.

\begin{enumerate}
    \item \textbf{Complete open-source implementation.}
          A coupled Brinkman--convection-diffusion topology
          optimisation framework in FEniCSx, including forward
          and continuous-adjoint formulations, installable
          as a Python package
          (Table~\ref{tab:comparison}, Section~\ref{sec:adjoint}).

    \item \textbf{Consistent SUPG stabilisation.}
          Standard SUPG~\cite{brooks1982,donea2003} applied to
          both forward and adjoint transport equations for
          convection-dominated regimes ($\Pe\sim10^2$--$10^5$),
          with numerical verification
          (Section~\ref{sec:supg_val}).

    \item \textbf{Reproducible software infrastructure.}
          Docker-based environments, continuous integration,
          automated testing, versioned releases, and archival
          through Zenodo
          (Table~\ref{tab:repro_checklist},
          Section~\ref{sec:reproducibility}).

    \item \textbf{Verification suite.}
          Gradient checks, regression tests, mesh sensitivity
          studies, and executable benchmark examples
          demonstrating framework generality across multiple
          target profiles
          (Figure~\ref{fig:gallery}, Table~\ref{tab:gallery}).

    \item \textbf{Modular software architecture.}
          Straightforward extension to alternative objectives,
          transport models, optimisation strategies, and
          inverse-design applications
          (Section~\ref{sec:extensibility}).
\end{enumerate}

\paragraph{Nature of contribution.}
This paper is a \emph{software implementation paper}:
a documented, tested, and reproducible open-source implementation
of adjoint-based topology optimisation for coupled
Brinkman--convection-diffusion systems in FEniCSx
(Table~\ref{tab:comparison}).

\paragraph{Scope and limitations.}
\texttt{brinkgrad}\footnote{The package is named \texttt{brinkgrad}
to denote its physical basis (Brinkman equations)
and avoid namespace collisions with existing libraries
(formerly \texttt{micrograd}) to avoid indexing ambiguity.}
operates within the Brinkman--SIMP porous-medium surrogate
on a fixed $80\times20$ mesh.
Two open directions are identified and quantified:
(i)~post-processing manufacturability analysis via robust projection
(\texttt{robust\_projection()}, implemented and demonstrated
in \texttt{examples/robust\_projection\_demo.py})~\cite{wang2011}
(thresholding results: Supplementary~S5); and
(ii)~mesh-independent continuation
(as expected for non-convex topology optimisation,
          finer meshes may find different local optima;
RMSE $0.091$--$0.107$ on $160\times40$
vs.\ $\rmseTopOpt$ on the $80\times20$ primary mesh).

The remainder of this paper is organised as follows.
Section~\ref{sec:model} presents the mathematical model and adjoint
derivation.
Section~\ref{sec:methods} describes the FEniCSx implementation and
configuration and continuation settings
(Section~\ref{sec:continuation}).
Section~\ref{sec:verification} presents verification studies.
Section~\ref{sec:results} presents the benchmark application.
Section~\ref{sec:discussion} discusses limitations and future directions,
and Section~\ref{sec:conclusion} concludes.